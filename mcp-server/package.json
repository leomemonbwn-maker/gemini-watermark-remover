{
  "name": "gemini-watermark-remover-mcp",
  "version": "1.0.0",
  "description": "Remote MCP server (Cloudflare Worker) that removes the visible Gemini AI sparkle watermark from images.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cf-wasm/photon": "^0.1.27",
    "@modelcontextprotocol/sdk": "^1.13.0",
    "agents": "^0.0.100",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.20.0"
  }
}
